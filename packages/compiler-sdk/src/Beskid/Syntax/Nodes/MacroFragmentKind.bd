// Generated by beskid_ast_reflect_gen (syntax nodes). Do not hand-edit.

/// Generated syntax node mirror: `crates/beskid_analysis/src/syntax/items/macro_definition.rs` — `MacroFragmentKind`.
///
/// **Rust documentation** (from mirrored type):
/// Fragment kind for a macro parameter (`block`, `expression`, …).
///
/// Generated index uses `beskid_doc.pest` `@variant` / `@par`; struct types list field names once (`@arg` is for callables only).
/// Implements `Node` via the host bridge; traverse with `Beskid.Compiler.Query` and `NodeRef`.
/// @variant(Block) unit (no payload)
/// @variant(Expression) unit (no payload)
/// @variant(Statement) unit (no payload)
/// @variant(Type) unit (no payload)
/// @variant(Identifier) unit (no payload)
/// @variant(Literal) unit (no payload)
/// @variant(Pattern) unit (no payload)
/// @variant(Path) unit (no payload)
/// @variant(Item) unit (no payload)
/// @variant(Node) unit (no payload)
pub enum MacroFragmentKind
{
    /// @variant(Block) unit variant (no payload).
    Block,
    /// @variant(Expression) unit variant (no payload).
    Expression,
    /// @variant(Statement) unit variant (no payload).
    Statement,
    /// @variant(Type) unit variant (no payload).
    Type,
    /// @variant(Identifier) unit variant (no payload).
    Identifier,
    /// @variant(Literal) unit variant (no payload).
    Literal,
    /// @variant(Pattern) unit variant (no payload).
    Pattern,
    /// @variant(Path) unit variant (no payload).
    Path,
    /// @variant(Item) unit variant (no payload).
    Item,
    /// @variant(Node) unit variant (no payload).
    Node,
}
