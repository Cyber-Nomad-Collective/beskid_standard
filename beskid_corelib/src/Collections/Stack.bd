/// LIFO stack shape and count helper.
pub type Stack<T> {
    /// Number of logical elements.
    i64 count,
}

/// Creates an empty stack.
pub Stack<T> New<T>() {
    return Stack<T> { count: 0 };
}

/// Returns the logical element count.
pub i64 Count<T>(Stack<T> stack) {
    return stack.count;
}
