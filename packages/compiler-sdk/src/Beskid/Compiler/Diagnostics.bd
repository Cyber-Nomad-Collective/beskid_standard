// CHECKED IN: regenerated by packages/compiler-sdk/regen_mod_sdk_surfaces.sh
// Rust sources in beskid_analysis are authoritative; Syntax/Nodes uses concrete helpers (no ReflectStub).
// --- diagnostic_kinds.rs ---

pub enum SemanticIssueKind
{
    AmbiguousImport(string name, string previous),
    AsyncKeywordReserved,
    AttributeTargetNotAllowed(string _attribute, string target, string allowed),
    AwaitKeywordReserved,
    BreakOutsideLoop,
    CompositionAmbiguousInject(string requested_type),
    CompositionChildScopeWithoutParent(string scope_name),
    CompositionDependencyCycle(string from_id, string to_id),
    CompositionDuplicateScopeName(string scope_name),
    CompositionHostInModProject,
    CompositionHostInheritanceCycle(string host_name),
    CompositionInjectOnConstructor,
    CompositionInvalidScopeQualifier(string qualifier),
    CompositionLaunchInLibProject,
    CompositionLaunchTargetNotHost(string target_name),
    CompositionMissingLaunchHost,
    CompositionMultipleLaunchHosts,
    CompositionOverrideLifetimeMismatch(string binding),
    CompositionScopedOutsideWith,
    CompositionUnresolvedInject(string requested_type),
    CompositionWithArgsMismatch(string scope_name),
    ConflictingEmbeddedContractMethod(string _contract_name, string method_name),
    ContinueOutsideLoop,
    ContractImplementationSignatureMismatch(string method_name, string expected, string actual),
    ContractMethodMissingImplementation(string _contract_name, string method_name, string expected),
    ContractMethodNotFound(string method_name, string receiver_name),
    DocArgOrReturnsOnNonCallable,
    DocDuplicateArgName(string name),
    DocDuplicateGenericName(string name),
    DocDuplicateVariantName(string name),
    DocParWithoutGenerics,
    DocReturnsOnUnit,
    DocUnknownArgName(string name),
    DocUnknownDirective(string name),
    DocUnknownGenericName(string name),
    DocUnknownVariantName(string name),
    DocUnresolvedRef(string path),
    DocVariantOnNonEnum,
    DuplicateAttributeDeclarationTarget(string target, string previous),
    DuplicateContractMethod(string name, string previous),
    DuplicateDefinitionName(string name, string previous),
    DuplicateEnumVariant(string name, string previous),
    DuplicateFileScopedModule(string module_path),
    DuplicateItemName(string name, string previous),
    DuplicatePatternBinding(string name),
    EnumConstructorArityMismatch(string expected, string actual),
    ExtendTypePrivateMemberAccess(string member_name, string _type_name, string private_span),
    FileScopedModuleNotFirstItem(string module_path),
    ImmutableAssignment(string name),
    InvalidHirSpan(string context),
    JoinWouldDeadlock,
    MacroAmbiguousName(string name),
    MacroArgumentArityMismatch(string name, string expected, string actual),
    MacroArgumentKindMismatch(string name, string parameter, string expected_kind),
    MacroDuplicateParameter(string name, string parameter),
    MacroExpansionDepthExceeded(string max_depth),
    MacroMetavariableOutsideBody(string name),
    MacroUnknown(string name),
    MatchArmTypeMismatch(string expected, string actual),
    MatchGuardMustBeBoolean,
    MatchNonExhaustive(string _enum_name),
    ModuleDeclarationForbiddenInFileScopedModule,
    NonNormalizedHirControlFlow(string message),
    PatternArityMismatch(string expected, string actual),
    QueryBoundsExceeded(string max_nodes, string max_depth),
    QueryNodeSpanUnavailable,
    QueryPipelineConflict,
    QueryPipelineStaleGeneration,
    ResolveDuplicateItem(string name, string previous),
    ResolveDuplicateLocal(string name, string previous),
    ResolveInvalidConformanceTarget(string name),
    ResolvePrivateItemInModule(string module_path, string name),
    ResolveShadowedLocal(string name, string previous),
    ResolveUnknownModulePath(string path),
    ResolveUnknownType(string name),
    ResolveUnknownTypeInModule(string module_path, string name),
    ResolveUnknownValue(string name),
    ResolveUnknownValueInModule(string module_path, string name),
    SpawnTargetNotFiberCompatible,
    StackReferenceEscapesSpawn,
    TypeCallArgumentMismatch(string expected_name, string actual_name),
    TypeCallArityMismatch(string expected, string actual),
    TypeEnumConstructorMismatch(string expected, string actual),
    TypeGenericArgumentMismatch(string expected, string actual),
    TypeImplicitNumericCast(string from, string to),
    TypeInvalidBinaryOp,
    TypeInvalidEventCapacity,
    TypeInvalidEventInvocationScope,
    TypeInvalidEventSubscriptionTarget,
    TypeInvalidMemberTarget,
    TypeInvalidTryTarget,
    TypeInvalidUnaryOp,
    TypeIterableNextArityMismatch(string expected, string actual),
    TypeIterableNextReturnNotOption,
    TypeIterableOptionSomeArityMismatch(string expected, string actual),
    TypeMatchArmMismatch(string expected_name, string actual_name),
    TypeMismatch(string expected_name, string actual_name),
    TypeMissingStructField(string name),
    TypeMissingTypeAnnotation(string name),
    TypeMissingTypeArguments,
    TypeNonBoolCondition,
    TypeNonIterableForTarget,
    TypeReturnMismatch(string expected_name, string actual_name),
    TypeUnknownCallTarget,
    TypeUnknownEnumType,
    TypeUnknownEnumVariant(string name),
    TypeUnknownStructField(string name),
    TypeUnknownStructType,
    TypeUnknownType,
    TypeUnknownValueType,
    TypeUnsupportedExpression,
    UnknownAttributeDeclarationTarget(string target, string allowed),
    UnknownEnumPath(string _enum_name, string variant_name),
    UnknownImportPath(string path),
    UnknownTypeInDefinition(string _type_name),
    UnqualifiedEnumConstructor(string variant_name, string _enum_name),
    UnreachableCode,
    UnresolvedHirTypePath,
    UnresolvedHirValuePath,
    UnusedImport(string path),
    UnusedPrivateItem(string name),
    UseBeforeDeclaration(string name),
    VisibilityModuleNotFound(string module_path, string file_candidate, string _mod_candidate),
    VisibilityViolationImportPrivate(string name, string private_span),
}

// --- diagnostics.rs ---

pub enum Severity
{
    Error,
    Note,
    Warning,
}

pub type string {}

// Hand-maintained facade ------------------------------------------------------
// Construction and reporting of compiler-native diagnostics from mod analyzers.

// Diagnostics facade contract version aligned with platform-spec Beskid.Compiler.Diagnostics hub.
pub string DiagnosticsFacadeVersion() {
    return "0.2.0";
}
