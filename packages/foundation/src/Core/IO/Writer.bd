use Core.IO.IoError;

/// A partial byte writer. Validate ranges before effects; valid empty writes bypass work.
/// Non-empty successful writes may transfer less than count. WriteAll detects zero progress.
/// @tier(standard)
pub contract Writer {
    Core.Results.Result<i64, IoError> Write(u8[] source, i64 offset, i64 count);
}
