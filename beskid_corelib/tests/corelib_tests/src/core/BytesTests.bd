use Core.Bytes.Slice;
use Testing.Assert;


test bytes_new_has_length {
    i64 buf = Core.Bytes.Slice.New(4);
    i64 len = Core.Bytes.Slice.Len(buf);
    Assert.Equal(len, 4, "bytes New length mismatch");
}
