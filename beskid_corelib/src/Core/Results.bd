/// Discriminated success/error carrier used across corelib.
///
/// Status: implemented — shape stable; variant payloads are language-level.

pub enum Result<TValue, TError> {
    Ok(TValue value),
    Error(TError error),
}
