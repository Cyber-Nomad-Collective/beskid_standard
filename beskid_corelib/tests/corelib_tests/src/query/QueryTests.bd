/// Iterator-backed `Query.Operators` smoke.

use Query.Operators;
use Testing.Assert;


test query_module_import_smoke {
    bool smoke = true;
    Assert.True(smoke, "Query operators import smoke until QueryState parity lands");
}
