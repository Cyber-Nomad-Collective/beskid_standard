/// Hub registration smoke.

use Concurrency.Hub;
use Testing.Assert;


test hub_module_import_smoke {
    Hub<i64> hub = Hub<i64>.Create();
    Assert.True(true, "Hub.Create smoke");
}

test hub_register_smoke {
    bool smoke = true;
    Assert.True(smoke, "Hub register deferred until Core.Results parity lands");
}
