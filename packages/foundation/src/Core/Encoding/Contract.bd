/// Encoder contract for byte-oriented encodings.
/// @tier(standard)
pub contract Encoder {
    Core.Results.Result<i64, Core.Encoding.EncodingError> EncodeToBytes(string text);
    Core.Results.Result<string, Core.Encoding.EncodingError> DecodeFromBytes(i64 data);
}
