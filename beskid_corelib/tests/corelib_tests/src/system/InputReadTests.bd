/// Smoke test for stdin read API surface (`Core.Input.Read`).

use Core.Results;
use Core.Input;

test input_read_smoke {
    Core.Results.Result<string, Core.Syscall.SyscallError> chunk = Core.Input.Read();
    match chunk {
        Result::Ok(_) => {},
        Result::Error(_) => {},
    };
}
