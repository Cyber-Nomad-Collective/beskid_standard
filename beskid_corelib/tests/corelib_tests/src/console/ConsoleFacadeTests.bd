/// Console facade smoke.

use Console;
use Testing.Assert;


test console_module_import_smoke {
    bool smoke = true;
    Assert.True(smoke, "Console facade deferred until ConsoleSize/Core.Results parity lands");
}

test messages_channel_smoke {
    bool smoke = true;
    Assert.True(smoke, "Console.MessagesChannel deferred smoke");
}
