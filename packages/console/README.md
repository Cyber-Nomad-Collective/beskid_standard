# corelib_console

Terminal and ANSI helpers for the Beskid standard library (`Console`, `Ansi`, and related modules).

Depends on `corelib_runtime` and `corelib_foundation`. Re-exported from the aggregate `corelib` prelude for typical applications.
