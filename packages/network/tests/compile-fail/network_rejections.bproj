network_rejections {
  name = "network_rejections"
  version = "0.1.0"
  root = "."
}

dependency "corelib_network" {
  source = path
  path = "../.."
}

target "RawHandle" {
  kind = Lib
  entry = "RawHandle.bd"
}

target "UdpIsNotStream" {
  kind = Lib
  entry = "UdpIsNotStream.bd"
}
