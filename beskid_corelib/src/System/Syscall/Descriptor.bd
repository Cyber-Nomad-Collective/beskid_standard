/// Descriptor selector for syscall wrappers.
pub enum Descriptor {
    /// Use one of the runtime-defined standard streams.
    Standard(System.Syscall.StandardStream stream),
    /// Use a raw numeric descriptor.
    Raw(i64 fd),
}
