/// Mutex wrapper surface smoke.

use Concurrency.Mutex;
use Testing.Assert;


test mutex_module_import_smoke {
    Mutex mutex = Mutex.Create();
    Assert.True(true, "Mutex.Create smoke");
}

test mutex_try_lock_smoke {
    bool smoke = true;
    Assert.True(smoke, "Mutex.TryLock deferred until Core.Optional import parity lands");
}
