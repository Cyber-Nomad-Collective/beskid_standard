// COMPILE-FAIL: ordinary programs cannot name retired runtime composition lookup exports.
// EXPECT: unresolved function `__composition_resolve`.
pub pointer DynamicCompositionLookupMustFail(pointer container, pointer key) {
    return __composition_resolve(container, key);
}
