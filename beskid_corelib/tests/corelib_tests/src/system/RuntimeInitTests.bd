/// Runtime.Init module tests — RegisterHandlers stable no-op contract.

use Testing.Assert;
use Runtime.Init;


test runtime_register_handlers_returns_zero {
    Assert.Equal(Init.RegisterHandlers(), 0, "RegisterHandlers is a stable no-op returning 0");
}
