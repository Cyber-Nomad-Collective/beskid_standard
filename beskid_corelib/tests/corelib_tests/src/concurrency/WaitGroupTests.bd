/// WaitGroup wrapper surface.

test wait_group_add_done_wait {
    Concurrency.WaitGroup group = Concurrency.WaitGroup.Create();
    group.Add(2);
    group.Done();
    group.Done();
    group.Wait();
    Testing.Assert.True(true, "wait group should complete after matching Done calls");
}
