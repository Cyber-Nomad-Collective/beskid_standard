/// Byte buffer error variants.
/// @tier(standard)
pub enum BytesError {
    /// A cursor transfer exceeds its configured capacity.
    OutOfBounds(),
    /// A range has negative components, overflows, or exceeds its source buffer.
    InvalidRange(),
}
