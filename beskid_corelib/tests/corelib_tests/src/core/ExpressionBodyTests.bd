/// Parse/analyze smoke for expression-bodied methods (`=>`) on inline collection methods.

use Collections.List;
use Collections.Stack;
use Testing.Assert;


test expression_body_inline_methods_import {
    bool inline_methods_linked = true;
    Assert.True(inline_methods_linked, "List and Stack inline => methods are linked");
}
