/// File system error variants for `Core.FS` helpers.
/// @tier(supported)
pub enum FsError {
    NotFound(string path),
    PermissionDenied(string path),
    IOError(string path),
    InvalidPath(string path),
    AlreadyExists(string path),
}
