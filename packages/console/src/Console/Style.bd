/// Shared layout/style enums for console controls.

pub enum Size {
    Small,
    Medium,
    Large,
}

pub enum TitleAlignment {
    Left,
    Center,
    Right,
}

pub enum Padding {
    None,
    Sized(Size size),
    Value(i32 value),
}

pub enum Margin {
    None,
    Sized(Size size),
    Value(i32 value),
}

/// Resolves `Size` to a character count for the given terminal width.
/// @arg(size) Logical size token.
/// @arg(columns) Terminal width in columns.
pub i32 ResolveSize(Size size, i32 columns) {
    match size {
        Size::Small => return 1,
        Size::Medium => return 2,
        Size::Large => return 4,
    }
    return 1;
}

pub i32 ResolvePadding(Padding padding, i32 columns) {
    match padding {
        Padding::None => return 0,
        Padding::Sized(s) => return ResolveSize(s, columns),
        Padding::Value(v) => return v,
    }
    return 0;
}

pub i32 ResolveMargin(Margin margin, i32 columns) {
    match margin {
        Margin::None => return 0,
        Margin::Sized(s) => return ResolveSize(s, columns),
        Margin::Value(v) => return v,
    }
    return 0;
}

pub i32 ClampWidth(i32 value, i32 minValue, i32 maxValue) {
    if value < minValue {
        return minValue;
    }
    if value > maxValue {
        return maxValue;
    }
    return value;
}
