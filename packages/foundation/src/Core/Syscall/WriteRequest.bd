/// Typed write request payload for syscall-based writes.
/// @par(descriptor) Logical sink for the write.
/// @par(data) UTF-8 payload handed to `__syscall_write`.
pub type WriteRequest {
    /// Descriptor selector for write target.
    Core.Syscall.Descriptor descriptor,
    /// UTF-8 data payload to write.
    string data,
}
