/// Fiber handle runtime wrapper smoke.

use Concurrency.Fiber;
use Testing.Assert;


test fiber_spawn_join_returns_value {
    bool smoke = true;
    Assert.True(smoke, "spawn/join API import smoke");
}

test fiber_module_compiles {
    return;
}
