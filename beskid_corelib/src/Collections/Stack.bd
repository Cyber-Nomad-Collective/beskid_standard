/// LIFO stack (shape only; storage stub).
///
/// Status: stub

pub type Stack<T> {
    i64 count,
}

pub Stack<T> New<T>() {
    return Stack<T> { count: 0 };
}

pub i64 Count<T>(Stack<T> stack) {
    return stack.count;
}
