/// Channel wrapper surface smoke.

use Concurrency.Channel;
use Testing.Assert;


test channel_module_import_smoke {
    Channel<i64> ch = Channel<i64>.Create();
    Assert.True(true, "Channel.Create smoke");
}

test channel_send_receive_smoke {
    bool smoke = true;
    Assert.True(smoke, "Channel Send/Receive deferred until Core.Results parity lands");
}
