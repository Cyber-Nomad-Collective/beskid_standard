/// WaitGroup wrapper surface.
use Concurrency.WaitGroup;

test wait_group_add_done_wait {
    WaitGroup group = WaitGroup.Create();
    group.Add(2);
    group.Done();
    group.Done();
    group.Wait();
    Testing.Assertions.AssertTrue(true, "wait group should complete after matching Done calls");
}
