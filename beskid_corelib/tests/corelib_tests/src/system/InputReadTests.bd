/// Smoke test for stdin read API surface (`System.Input.Read`).
test input_read_smoke {
    Core.Results.Result<string, System.Syscall.SyscallError> chunk = System.Input.Read();
    match chunk {
        Result::Ok(_) => {},
        Result::Error(_) => {},
    };
}
