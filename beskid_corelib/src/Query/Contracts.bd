/// Query-facing option and iterator contracts.
///
/// Status: partial — types present; execution semantics evolving.

pub enum Option {
    Some(i64 value),
    None,
}

pub contract Iterator {
    Option Next();
}

pub bool HasValue(Option value) {
    return match value {
        Option::Some(_) => true,
        Option::None => false,
    };
}
