# corelib_concurrency

Cooperative concurrency primitives for the Beskid standard library (`Fiber`, channels, and related modules under `System.Threading`).

Depends on `corelib_foundation`. OS-thread helpers live in this package; the aggregate `corelib` prelude re-exports the supported surface.
