/// Markdown subset parsing smoke for `Console.Format`.

use Console.Format;
use Testing.Assert;


test format_module_imports {
    bool smoke = true;
    Assert.True(smoke, "Console.Format module import smoke");
}
